const String prefsHighScoreKey = 'highScore';

// Durations (can be adjusted)
const Duration levelAnnouncementDuration = Duration(seconds: 2);
const Duration flashRevertDelay = Duration(milliseconds: 300);